{
  "name": "@blotter/database",
  "version": "1.0.0",
  "private": true,
  "description": "Postgres schema, migrations and the migration runner",
  "type": "module",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate deploy",
    "migrate:status": "prisma migrate status"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "prisma": "^7.10.0"
  }
}
